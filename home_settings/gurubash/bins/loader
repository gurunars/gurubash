# Add home bin or bins directory if it exists to the path
DIR=$HOME/bin
if [ -d "$DIR" ]; then
    export PATH=$PATH:$DIR
    return
fi

DIR=$HOME/bins
if [ -d "$DIR" ]; then
    export PATH=$PATH:$DIR
    return
fi
